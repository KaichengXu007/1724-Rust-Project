#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // Minimal stub binary used during development; the real server is started via tests or a dedicated binary.
    println!("server stub (no-op)");
    Ok(())
}
